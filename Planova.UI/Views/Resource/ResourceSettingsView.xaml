<UserControl x:Class="Planova.UI.Views.Resource.ResourceSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Resource Settings" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="0,0,16,0">
                <Border BorderThickness="1" BorderBrush="Gray" CornerRadius="4" Padding="12" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Defaults" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Default Currency" Margin="0,0,12,8" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding DefaultCurrency}" Margin="0,0,0,8" MinWidth="150"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Default Unit of Measure" Margin="0,0,12,8" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DefaultUnitOfMeasure}" Margin="0,0,0,8" MinWidth="150"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Escalation Rate (%)" Margin="0,0,12,8" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding EscalationRate}" Margin="0,0,0,8" MinWidth="150"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border BorderThickness="1" BorderBrush="Gray" CornerRadius="4" Padding="12" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Behavior" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        <CheckBox Content="Use organization standard rates" IsChecked="{Binding UseOrganizationStandardRates}" Margin="0,0,0,8"/>
                        <CheckBox Content="Auto-apply escalation" IsChecked="{Binding AutoApplyEscalation}" Margin="0,0,0,8"/>
                        <CheckBox Content="Show inactive resources" IsChecked="{Binding ShowInactiveResources}" Margin="0,0,0,8"/>
                        <CheckBox Content="Enable bulk rate editing" IsChecked="{Binding EnableBulkRateEditing}" Margin="0,0,0,8"/>
                        <CheckBox Content="Enable crew blended rate" IsChecked="{Binding EnableCrewBlendedRate}" Margin="0,0,0,8"/>
                    </StackPanel>
                </Border>

                <Border BorderThickness="1" BorderBrush="Gray" CornerRadius="4" Padding="12">
                    <StackPanel>
                        <TextBlock Text="AI Estimation" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                        <CheckBox Content="Enable AI estimation" IsChecked="{Binding EnableAiEstimation}" Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Provider Endpoint" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding AiProviderEndpoint, UpdateSourceTrigger=PropertyChanged}" MinWidth="150"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
