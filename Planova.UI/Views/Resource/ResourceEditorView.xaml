<UserControl x:Class="Planova.UI.Views.Resource.ResourceEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.Resource"
             d:DataContext="{d:DesignInstance vm:ResourceEditorViewModel}"
             Background="{DynamicResource WorkspaceBackgroundGradient}"
             FontFamily="{DynamicResource BrandFontFamily}"
             mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="12" MaxWidth="600">
            <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold"
                       Foreground="{DynamicResource ThemeForeground}"/>

            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
            <ComboBox SelectedItem="{Binding ResourceType, Mode=TwoWay}" />
            <ComboBox SelectedItem="{Binding Scope, Mode=TwoWay}" />
            <TextBox Text="{Binding DefaultRate, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Text="{Binding UnitOfMeasure, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Text="{Binding Currency, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" TextWrapping="Wrap" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Command="{Binding SaveCommand}" />
                <Button Content="Cancel" Command="{Binding CancelCommand}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
