<UserControl x:Class="Planova.UI.Views.Resource.ResourceAiEstimationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.Resource"
             d:DataContext="{d:DesignInstance vm:ResourceAiEstimationViewModel}"
             Background="{DynamicResource WorkspaceBackgroundGradient}"
             FontFamily="{DynamicResource BrandFontFamily}"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="AI Resource Estimation" FontSize="20" FontWeight="Bold"
                       Foreground="{DynamicResource ThemeForeground}" Margin="0,0,16,0"/>
            <Button Content="Estimate Resources" Command="{Binding EstimateResourcesCommand}"
                    Margin="0,0,8,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{DynamicResource DangerBrush}"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="4"
                     Visibility="{Binding IsEstimating, Converter={StaticResource BoolToVis}}"
                     Margin="0,0,0,8"/>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Suggestions}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Background="Transparent"
                  BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Resource" Binding="{Binding ResourceName}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding SuggestedQuantity}" Width="80"/>
                <DataGridTextColumn Header="UOM" Binding="{Binding UnitOfMeasure}" Width="80"/>
                <DataGridTextColumn Header="Confidence" Binding="{Binding ConfidenceScore}" Width="80"/>
                <DataGridTextColumn Header="Reasoning" Binding="{Binding Reasoning}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Accept All" Command="{Binding AcceptAllCommand}" Margin="0,0,8,0"/>
            <Button Content="Reject All" Command="{Binding RejectAllCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
