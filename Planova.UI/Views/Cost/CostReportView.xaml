<UserControl x:Class="Planova.UI.Views.Cost.CostReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.Cost"
             d:DataContext="{d:DesignInstance vm:CostReportViewModel}"
             Background="{DynamicResource WorkspaceBackgroundGradient}"
             FontFamily="{DynamicResource BrandFontFamily}"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Cost Reports" FontSize="18" FontWeight="SemiBold"
                   Foreground="{DynamicResource ThemeForeground}" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <ComboBox ItemsSource="{Binding ReportTypes}"
                      SelectedItem="{Binding SelectedReportType, Mode=TwoWay}"
                      Width="200" Margin="0,0,8,0"
                      FontFamily="{DynamicResource BrandFontFamily}"/>
            <Button Content="Generate" Command="{Binding GenerateReportCommand}" Margin="0,0,8,0"
                    FontFamily="{DynamicResource BrandFontFamily}"/>
            <Button Content="Export Excel" Command="{Binding ExportExcelCommand}" Margin="0,0,8,0"
                    FontFamily="{DynamicResource BrandFontFamily}"/>
            <Button Content="Export PDF" Command="{Binding ExportPdfCommand}"
                    FontFamily="{DynamicResource BrandFontFamily}"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Margin="0,0,0,8"
                   Foreground="{DynamicResource ThemeForeground}"/>

        <ScrollViewer Grid.Row="3">
            <TextBlock Text="Report preview will be displayed here."/>
        </ScrollViewer>
    </Grid>
</UserControl>
