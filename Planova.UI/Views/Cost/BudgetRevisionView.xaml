<UserControl x:Class="Planova.UI.Views.Cost.BudgetRevisionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.Cost"
             d:DataContext="{d:DesignInstance vm:BudgetRevisionViewModel}"
             Background="{DynamicResource WorkspaceBackgroundGradient}"
             FontFamily="{DynamicResource BrandFontFamily}"
             mc:Ignorable="d">
    <Grid>
        <DataGrid ItemsSource="{Binding Revisions}"
                  SelectedItem="{Binding SelectedRevision, Mode=TwoWay}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Background="{DynamicResource ThemeBackgroundMedium}"
                  BorderBrush="{DynamicResource ThemeBorder}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding RevisionNumber}" Width="50"/>
                <DataGridTextColumn Header="Type" Binding="{Binding RevisionType}" Width="100"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="*"/>
                <DataGridTextColumn Header="Approved By" Binding="{Binding ApprovedBy}" Width="120"/>
                <DataGridTextColumn Header="Approved At" Binding="{Binding ApprovedAt}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
