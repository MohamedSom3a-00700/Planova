<UserControl x:Class="Planova.UI.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels"
             xmlns:conv="clr-namespace:Planova.UI.Converters"
             d:DataContext="{d:DesignInstance vm:DashboardViewModel}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <ScrollViewer>
        <StackPanel Margin="24">
            <TextBlock Text="Dashboard" FontSize="24" FontWeight="Bold"
                       Foreground="{DynamicResource ThemeForeground}"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red"
                       Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"
                       Margin="0,8,0,0"/>

            <WrapPanel Margin="0,24,0,0">
                <Border Background="{DynamicResource ThemeSurface}" CornerRadius="8"
                        Width="200" Height="120" Margin="0,0,16,16">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding TotalProjects}" FontSize="36" FontWeight="Bold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Projects" FontSize="14" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Background="{DynamicResource ThemeSurface}" CornerRadius="8"
                        Width="200" Height="120" Margin="0,0,16,16">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding TotalClients}" FontSize="36" FontWeight="Bold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Clients" FontSize="14" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Background="{DynamicResource ThemeSurface}" CornerRadius="8"
                        Width="200" Height="120" Margin="0,0,16,16">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding TotalContracts}" FontSize="36" FontWeight="Bold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Contracts" FontSize="14" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <TextBlock Text="Status Distribution" FontSize="16" FontWeight="SemiBold" Margin="0,16,0,8"/>
            <TextBlock Text="{Binding StatusDistributionSummary}" TextWrapping="Wrap" Margin="0,0,0,16"/>

            <TextBlock Text="Quick Actions" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,8"/>
            <StackPanel Orientation="Horizontal">
                <Button Content="+ New Project" Command="{Binding NavigateToProjectsCommand}"
                        Height="32" Width="120" Margin="0,0,8,0"/>
                <Button Content="+ New Client" Command="{Binding NavigateToClientsCommand}"
                        Height="32" Width="120" Margin="0,0,8,0"/>
                <Button Content="+ New Contract" Command="{Binding NavigateToContractsCommand}"
                        Height="32" Width="120"/>
            </StackPanel>

            <TextBlock Text="Recent Activity" FontSize="16" FontWeight="SemiBold" Margin="0,24,0,8"/>
            <ListView ItemsSource="{Binding RecentActivity}" Background="{DynamicResource ThemeSurface}"
                      BorderThickness="0" MaxHeight="300">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="{Binding EntityType}" FontWeight="SemiBold" Width="80"/>
                            <TextBlock Text="{Binding EntityName}" Width="200"/>
                            <TextBlock Text="{Binding Action}" Width="80"/>
                            <TextBlock Text="{Binding Timestamp, StringFormat={}{0:g}}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</UserControl>
