<UserControl x:Class="Planova.UI.Views.NavigationRail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="48"
             Background="{DynamicResource ThemeSurface}">
    <StackPanel>
        <Button x:Name="ToggleButton" Content="☰" Width="48" Height="48"
                Background="Transparent"
                Foreground="{DynamicResource ThemeForeground}"
                BorderThickness="0"
                Click="OnToggleClick" />
        <ItemsControl x:Name="NavItems">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding DisplayName}" Width="48" Height="48"
                            Background="Transparent"
                            Foreground="{DynamicResource ThemeForeground}"
                            BorderThickness="0"
                            Command="{Binding Command}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
