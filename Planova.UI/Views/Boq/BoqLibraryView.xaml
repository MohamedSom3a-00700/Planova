<UserControl x:Class="Planova.UI.Views.Boq.BoqLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Item Libraries" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,12,0"
                        Visibility="{Binding HasBoq, Converter={StaticResource BoolToVis}}">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="Refresh" Command="{Binding LoadLibrariesCommand}" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
                </StackPanel>
                <ListBox ItemsSource="{Binding Libraries}"
                         SelectedItem="{Binding SelectedLibrary, Mode=TwoWay}"
                         DisplayMemberPath="Name"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Visibility="{Binding HasBoq, Converter={StaticResource BoolToVis}}">
                <GroupBox Header="Add Library" Margin="0,0,0,8">
                    <StackPanel>
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Name:" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding NewName}"/>
                        </Grid>
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Description:" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding NewDescription}"/>
                        </Grid>
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Type:" VerticalAlignment="Center"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding LibraryTypeOptions}"
                                      SelectedItem="{Binding NewLibraryType}"/>
                        </Grid>
                        <Button Content="Add" Command="{Binding AddLibraryCommand}" MinWidth="80"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Selected Library" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="{Binding SelectedLibrary.Name, StringFormat='Name: {0}'}"/>
                        <TextBlock Text="{Binding SelectedLibrary.Description, StringFormat='Description: {0}'}"/>
                        <TextBlock Text="{Binding SelectedLibrary.ItemCount, StringFormat='Items: {0}'}"/>
                        <Button Content="Delete" Command="{Binding DeleteLibraryCommand}"
                                Margin="0,8,0,0" MinWidth="80"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
