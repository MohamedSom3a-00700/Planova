<UserControl x:Class="Planova.UI.Views.Wbs.WbsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="WBS List" FontSize="20" FontWeight="Bold" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,8,0"
                     ToolTip="Search WBS"/>
            <Button Content="Search" Command="{Binding LoadWbsListCommand}" Margin="0,0,8,0"/>
            <Separator Width="1" Background="Gray" Margin="0,0,8,0"/>
            <TextBox Text="{Binding NewWbsName, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,8,0"
                     ToolTip="New WBS name"/>
            <Button Content="Create WBS" Command="{Binding CreateWbsCommand}" Margin="0,0,8,0"/>
            <Button Content="Delete WBS" Command="{Binding DeleteWbsCommand}" Margin="0,0,8,0"/>
            <Button Content="Rename" Command="{Binding RenameWbsCommand}" Margin="0,0,8,0"/>
        </StackPanel>

        <ListView Grid.Row="2" ItemsSource="{Binding WbsItems}"
                  SelectedItem="{Binding SelectedWbs, Mode=TwoWay}"
                  VirtualizingPanel.ScrollUnit="Item"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="160" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Status" Width="80" DisplayMemberBinding="{Binding Status}"/>
                    <GridViewColumn Header="Source" Width="90" DisplayMemberBinding="{Binding Source}"/>
                    <GridViewColumn Header="Revision" Width="65" DisplayMemberBinding="{Binding Revision}"/>
                    <GridViewColumn Header="Items" Width="50" DisplayMemberBinding="{Binding ItemCount}"/>
                    <GridViewColumn Header="Weight" Width="70" DisplayMemberBinding="{Binding TotalWeight}"/>
                    <GridViewColumn Header="Created" Width="110" DisplayMemberBinding="{Binding CreatedAt}"/>
                    <GridViewColumn Header="Modified" Width="110" DisplayMemberBinding="{Binding UpdatedAt}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
