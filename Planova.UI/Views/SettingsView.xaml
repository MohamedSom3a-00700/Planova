<UserControl x:Class="Planova.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Settings" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="General" Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Display Name:" VerticalAlignment="Center" Margin="0,0,0,6"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding DisplayName}" Margin="0,0,0,6" Width="300" HorizontalAlignment="Left"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Organization:" VerticalAlignment="Center" Margin="0,0,0,6"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Organization}" Margin="0,0,0,6" Width="300" HorizontalAlignment="Left"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Role / Title:" VerticalAlignment="Center"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding RoleLabel}" Width="300" HorizontalAlignment="Left"/>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Appearance" Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Theme:" VerticalAlignment="Center" Margin="0,0,0,6"/>
                        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding ThemeOptions}"
                                  SelectedItem="{Binding SelectedTheme}" Margin="0,0,0,6" Width="160" HorizontalAlignment="Left"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Language:" VerticalAlignment="Center"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding LanguageOptions}"
                                  SelectedItem="{Binding SelectedLanguage}" Width="160" HorizontalAlignment="Left"/>
                    </Grid>
                </GroupBox>

                <GroupBox Header="Database" Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Location:" VerticalAlignment="Top" Margin="0,4,0,6"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding DbLocation, Mode=OneWay}"
                                 IsReadOnly="True" Margin="0,0,0,6"
                                 Background="{DynamicResource TextBoxBackground.Disabled}"
                                 Foreground="Gray" FontFamily="Consolas" FontSize="12"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Size:" VerticalAlignment="Center" Margin="0,0,0,6"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DbSize, Mode=OneWay}"
                                 IsReadOnly="True" Margin="0,0,0,6" Width="120" HorizontalAlignment="Left"
                                 Background="{DynamicResource TextBoxBackground.Disabled}"
                                 Foreground="Gray"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Connection String:" VerticalAlignment="Top" Margin="0,4,0,0"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ConnectionString, Mode=OneWay}"
                                 IsReadOnly="True"
                                 Background="{DynamicResource TextBoxBackground.Disabled}"
                                 Foreground="Gray" FontFamily="Consolas" FontSize="12"
                                 TextWrapping="NoWrap"/>
                    </Grid>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" MinWidth="100" Margin="0,0,8,0"/>
            <Button Content="Reset Defaults" Command="{Binding ResetDefaultsCommand}" MinWidth="100" Margin="0,0,16,0"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</UserControl>
