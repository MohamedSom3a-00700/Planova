<UserControl x:Class="Planova.UI.Views.Activity.ActivityListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.Activity"
             d:DataContext="{d:DesignInstance vm:ActivityListViewModel}"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Width="200" Margin="0,0,8,0"/>
            <ComboBox Text="{Binding StatusFilter}" Width="120" Margin="0,0,8,0"/>
            <ComboBox Text="{Binding TypeFilter}" Width="120" Margin="0,0,8,0"/>
            <Button Content="Load" Command="{Binding LoadActivitiesCommand}" Margin="0,0,8,0"/>
            <Button Content="Delete" Command="{Binding DeleteActivityCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Activities}"
                  SelectedItem="{Binding SelectedActivity}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="80"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding ActivityType}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="60"/>
                <DataGridTextColumn Header="Planned Start" Binding="{Binding PlannedStart, StringFormat={}{0:d}}" Width="100"/>
                <DataGridTextColumn Header="Planned Finish" Binding="{Binding PlannedFinish, StringFormat={}{0:d}}" Width="100"/>
                <DataGridTextColumn Header="%" Binding="{Binding PercentComplete}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
