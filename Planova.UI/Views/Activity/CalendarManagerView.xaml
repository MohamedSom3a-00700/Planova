<UserControl x:Class="Planova.UI.Views.Activity.CalendarManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.Activity"
             xmlns:actViews="clr-namespace:Planova.UI.Views.Activity"
             d:DataContext="{d:DesignInstance vm:CalendarManagerViewModel}"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,8" Foreground="Gray">
            Manage project calendars and working days. Click a day cell to toggle working/non-working.
            Use bulk range to mark multiple consecutive days as non-working.
        </TextBlock>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="0,0,8,0" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Calendars" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="+ New" Command="{Binding CreateCalendarCommand}" Height="24"/>
                </Grid>
                <ListBox Grid.Row="1" ItemsSource="{Binding Calendars}" SelectedItem="{Binding SelectedCalendar}"
                         DisplayMemberPath="Name"/>
            </Grid>
        </Border>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="&lt;" Command="{Binding PreviousMonthCommand}" Width="30" Margin="0,0,4,0"/>
                    <TextBlock Text="{Binding CurrentMonthLabel}" VerticalAlignment="Center" Width="120"
                               TextAlignment="Center" FontWeight="Bold"/>
                    <Button Content="&gt;" Command="{Binding NextMonthCommand}" Width="30" Margin="4,0,0,0"/>
                </StackPanel>
                <actViews:CalendarDayGridView Grid.Row="1" DataContext="{Binding}"/>
            </Grid>
        </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Import Non-Working Days from Excel" Command="{Binding ImportNonWorkingDaysFromExcelCommand}" Margin="0,0,8,0"/>
            <Button Content="Get from AI" Command="{Binding GetNonWorkingDaysFromAiCommand}" Margin="0,0,8,0"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</UserControl>
