<UserControl x:Class="Planova.UI.Views.ScheduleComparison.ScheduleComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.ScheduleComparison"
             xmlns:views="clr-namespace:Planova.UI.Views.ScheduleComparison"
             d:DataContext="{d:DesignInstance vm:ScheduleComparisonViewModel}"
             mc:Ignorable="d"
             FlowDirection="{Binding FlowDirection, RelativeSource={RelativeSource Self}}">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:CompareViewModel}">
            <views:CompareView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ActivityDiffViewModel}">
            <views:ActivityDiffView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LogicDiffViewModel}">
            <views:LogicDiffView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ResourceDiffViewModel}">
            <views:ResourceDiffView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CriticalPathDiffViewModel}">
            <views:CriticalPathDiffView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:FloatImpactViewModel}">
            <views:FloatImpactView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ComparisonHistoryViewModel}">
            <views:ComparisonHistoryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ComparisonExportViewModel}">
            <views:ComparisonExportView />
        </DataTemplate>
    </UserControl.Resources>
    <TabControl ItemsSource="{Binding Tabs}"
                SelectedItem="{Binding SelectedTab}"
                Background="Transparent" BorderThickness="0">
        <TabControl.Resources>
            <Style TargetType="TabItem">
                <Setter Property="IsEnabled" Value="{Binding IsEnabled}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Padding" Value="10,6" />
            </Style>
        </TabControl.Resources>
        <TabControl.ItemTemplate>
            <DataTemplate DataType="{x:Type vm:ScheduleComparisonTab}">
                <TextBlock Text="{Binding Header}" />
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>
            <DataTemplate DataType="{x:Type vm:ScheduleComparisonTab}">
                <ContentControl Content="{Binding Content}" />
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</UserControl>
