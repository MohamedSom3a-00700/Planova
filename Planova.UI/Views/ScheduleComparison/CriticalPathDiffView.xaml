<UserControl x:Class="Planova.UI.Views.ScheduleComparison.CriticalPathDiffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.ScheduleComparison"
             d:DataContext="{d:DesignInstance vm:CriticalPathDiffViewModel}"
             mc:Ignorable="d"
             FlowDirection="{Binding FlowDirection, RelativeSource={RelativeSource Self}}">
    <StackPanel Margin="20">
        <TextBlock Text="Critical Path Analysis" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
        <TextBlock Text="{Binding SourceDuration, StringFormat='Source Duration: {0} days'}" />
        <TextBlock Text="{Binding TargetDuration, StringFormat='Target Duration: {0} days'}" />
        <TextBlock Text="{Binding DurationChange, StringFormat='Change: {0} days'}" />
    </StackPanel>
</UserControl>
