<UserControl x:Class="Planova.UI.Views.ScheduleComparison.ComparisonExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.ScheduleComparison"
             d:DataContext="{d:DesignInstance vm:ComparisonExportViewModel}"
             mc:Ignorable="d"
             FlowDirection="{Binding FlowDirection, RelativeSource={RelativeSource Self}}">
    <StackPanel Margin="20">
        <TextBlock Text="Export Comparison Results" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20" />
        <TextBlock Text="{Binding StatusMessage}" Margin="0,0,0,10" />
        <Button Content="Export to Excel" Command="{Binding ExportToExcelCommand}" Margin="0,0,0,10" />
        <Button Content="Export to PDF" Command="{Binding ExportToPdfCommand}" Margin="0,0,0,10" />
        <Button Content="Export to JSON" Command="{Binding ExportToJsonCommand}" Margin="0,0,0,10" />
        <TextBlock Text="{Binding ExportPath}" TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
