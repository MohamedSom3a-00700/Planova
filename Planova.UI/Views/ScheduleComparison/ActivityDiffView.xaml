<UserControl x:Class="Planova.UI.Views.ScheduleComparison.ActivityDiffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.ScheduleComparison"
             d:DataContext="{d:DesignInstance vm:ActivityDiffViewModel}"
             mc:Ignorable="d"
             FlowDirection="{Binding FlowDirection, RelativeSource={RelativeSource Self}}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Activity Diffs" FontSize="16" FontWeight="SemiBold" />
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Diffs}" AutoGenerateColumns="True"
                  IsReadOnly="True" />
    </Grid>
</UserControl>
