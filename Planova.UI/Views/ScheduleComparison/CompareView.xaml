<UserControl x:Class="Planova.UI.Views.ScheduleComparison.CompareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.ScheduleComparison"
             d:DataContext="{d:DesignInstance vm:CompareViewModel}"
             mc:Ignorable="d"
             FlowDirection="{Binding FlowDirection, RelativeSource={RelativeSource Self}}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Schedule Comparison" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Source:" VerticalAlignment="Center" Margin="0,0,10,10" />
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,10">
            <CheckBox Content="Use Primavera Import (XER)"
                      IsChecked="{Binding UsePrimaveraSource}"
                      IsEnabled="{Binding IsPrimaveraAvailable}" />
            <ComboBox ItemsSource="{Binding SourceSnapshots}" SelectedItem="{Binding SelectedSource}"
                      DisplayMemberPath="Label" MinWidth="250" />
        </StackPanel>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Target:" VerticalAlignment="Center" Margin="0,0,10,10" />
        <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
            <CheckBox Content="Use Primavera Import (XER)"
                      IsChecked="{Binding UsePrimaveraTarget}"
                      IsEnabled="{Binding IsPrimaveraAvailable}" />
            <ComboBox ItemsSource="{Binding TargetSnapshots}" SelectedItem="{Binding SelectedTarget}"
                      DisplayMemberPath="Label" MinWidth="250" />
        </StackPanel>

        <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Text="{Binding StatusMessage}" Margin="0,0,0,10" />

        <Button Grid.Row="4" Grid.ColumnSpan="2" Content="Run Comparison" Command="{Binding RunComparisonCommand}"
                HorizontalAlignment="Left" Margin="0,0,0,10" />
    </Grid>
</UserControl>
