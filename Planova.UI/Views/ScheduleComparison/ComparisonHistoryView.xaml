<UserControl x:Class="Planova.UI.Views.ScheduleComparison.ComparisonHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.ScheduleComparison"
             d:DataContext="{d:DesignInstance vm:ComparisonHistoryViewModel}"
             mc:Ignorable="d"
             FlowDirection="{Binding FlowDirection, RelativeSource={RelativeSource Self}}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Comparison History" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Margin="0,0,0,10" />
        <DataGrid Grid.Row="2" ItemsSource="{Binding Sessions}" SelectedItem="{Binding SelectedSession}"
                  AutoGenerateColumns="True" IsReadOnly="True" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Re-Open" Command="{Binding ReOpenSessionCommand}" Margin="0,0,10,0" />
            <Button Content="Re-Export" Command="{Binding ReExportSessionCommand}" Margin="0,0,10,0" />
            <Button Content="Delete" Command="{Binding DeleteSessionCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
