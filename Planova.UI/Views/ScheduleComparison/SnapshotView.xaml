<UserControl x:Class="Planova.UI.Views.ScheduleComparison.SnapshotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.ScheduleComparison"
             d:DataContext="{d:DesignInstance vm:SnapshotViewModel}"
             mc:Ignorable="d"
             FlowDirection="{Binding FlowDirection, RelativeSource={RelativeSource Self}}">
    <StackPanel Margin="20">
        <TextBlock Text="Schedule Snapshots" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" />
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Text="{Binding NewSnapshotLabel, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,10,0" />
            <Button Content="Capture" Command="{Binding CaptureSnapshotCommand}" />
        </StackPanel>
        <TextBlock Text="{Binding StatusMessage}" Margin="0,0,0,10" />
        <ListBox ItemsSource="{Binding Snapshots}" DisplayMemberPath="Label" />
    </StackPanel>
</UserControl>
