<UserControl x:Class="Planova.UI.Views.Primavera.PrimaveraRepairView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.Primavera"
             xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:PrimaveraRepairViewModel}">
  <Grid Margin="20,16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
      <ui:SymbolIcon Symbol="Wrench24" FontSize="20" Margin="0,0,8,0"
                     Foreground="{DynamicResource ThemeForeground}" />
      <StackPanel>
        <TextBlock Text="Repair Schedule Issues" FontSize="18" FontWeight="SemiBold" />
        <TextBlock Text="Review and apply suggested fixes for detected issues" FontSize="12"
                   Foreground="{DynamicResource BrandTextSecondary}" />
      </StackPanel>
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
      <ui:Button Command="{Binding LoadSuggestionsCommand}" Appearance="Primary"
                 Content="Load Suggestions" Width="140" Margin="0,0,8,0" />
      <ui:Button Command="{Binding ApplyAllFixesCommand}"
                 Content="Apply All Fixes" Width="130" />
    </StackPanel>

    <Border Grid.Row="2" Background="{DynamicResource ThemeCard}" Padding="8" CornerRadius="6"
            BorderBrush="{DynamicResource ThemeBorder}" BorderThickness="1">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding StatusMessage}" Margin="4,4,0,8" FontSize="12"
                   Foreground="{DynamicResource ThemeForeground}" />
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding SuggestedFixes}"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  BorderBrush="{DynamicResource ThemeBorder}"
                  BorderThickness="1"
                  RowHeight="26"
                  FontSize="12">
          <DataGrid.Resources>
            <Style TargetType="DataGridColumnHeader">
              <Setter Property="FontWeight" Value="SemiBold" />
              <Setter Property="Padding" Value="6,3" />
            </Style>
            <Style TargetType="DataGridCell">
              <Setter Property="Padding" Value="6,2" />
            </Style>
          </DataGrid.Resources>
        </DataGrid>
      </Grid>
    </Border>
  </Grid>
</UserControl>
