<UserControl x:Class="Planova.UI.Views.Primavera.PrimaveraActivitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Planova.UI.ViewModels.Primavera"
             xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:PrimaveraActivitiesViewModel}">
  <Grid Margin="4">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Border Background="{DynamicResource ThemeCard}" Padding="8,6" CornerRadius="4" Margin="0,0,0,6"
            BorderBrush="{DynamicResource ThemeBorder}" BorderThickness="1">
      <StackPanel Orientation="Horizontal">
        <ui:SymbolIcon Symbol="Search24" FontSize="14" Margin="0,0,6,0"
                       Foreground="{DynamicResource BrandTextSecondary}" VerticalAlignment="Center" />
        <TextBox Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Width="280" ToolTip="Filter by name, task ID, or status" />
        <TextBlock Text="Activities" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"
                   Margin="16,0,0,0" />
      </StackPanel>
    </Border>
    <DataGrid Grid.Row="1"
              ItemsSource="{Binding Activities}"
              AutoGenerateColumns="True"
              IsReadOnly="False"
              VirtualizingPanel.VirtualizationMode="Recycling"
              EnableRowVirtualization="True"
              EnableColumnVirtualization="True"
              BorderBrush="{DynamicResource ThemeBorder}"
              BorderThickness="1"
              RowHeight="26"
              FontSize="12">
      <DataGrid.Resources>
        <Style TargetType="DataGridColumnHeader">
          <Setter Property="FontWeight" Value="SemiBold" />
          <Setter Property="Padding" Value="6,3" />
        </Style>
        <Style TargetType="DataGridCell">
          <Setter Property="Padding" Value="6,2" />
        </Style>
      </DataGrid.Resources>
    </DataGrid>
  </Grid>
</UserControl>
