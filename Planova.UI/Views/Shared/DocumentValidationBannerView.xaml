<UserControl x:Class="Planova.UI.Views.Shared.DocumentValidationBannerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:Planova.UI.Converters"
             mc:Ignorable="d"
             Height="Auto">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Border Background="{DynamicResource ThemeCard}"
            BorderBrush="{DynamicResource BrandWarning}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="8,6"
            Visibility="{Binding HasWarning, Converter={StaticResource BoolToVis}}"
            MinHeight="36">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="⚠" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"
                       Foreground="{DynamicResource BrandWarning}"/>
            <TextBlock Grid.Column="1" Text="{Binding WarningMessage}"
                       VerticalAlignment="Center" FontSize="12"
                       TextTrimming="CharacterEllipsis"
                       Foreground="{DynamicResource ThemeForeground}"/>
            <Button Grid.Column="2" Content="Go to Project Documents"
                    Height="26" FontSize="11"
                    Padding="8,0"
                    Command="{Binding GoToProjectDocumentsCommand}"/>
        </Grid>
    </Border>
</UserControl>
