<UserControl x:Class="Planova.UI.Views.Empty.EmptyStateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="400">
    <Border Style="{DynamicResource EmptyStateContainerStyle}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <ui:SymbolIcon Symbol="{Binding IconGlyph}"
                           FontSize="48"
                           Foreground="{DynamicResource BrandTextSecondary}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,12" />
            <TextBlock Text="{Binding Title}"
                       Style="{DynamicResource EmptyStateTitleStyle}" />
            <TextBlock Text="{Binding Description}"
                       Style="{DynamicResource EmptyStateDescriptionStyle}" />
        </StackPanel>
    </Border>
</UserControl>
